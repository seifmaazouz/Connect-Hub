package connecthub.backend.utils.errors;

public class Alert {
    public static enum alerts {
        INVALID_EMAIL_FORMAT, INVALID_PASSWORD, USER_EMAIL_EXISTS, PROCESS_FAILED, PROCESS_SUCCEEDED, USER_NAME_EXISTS
    }
}
