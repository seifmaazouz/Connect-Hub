package connecthub.contentCreation.backend;

public class PostDatabase extends ContentDatabase<Post> {

    public PostDatabase(ContentLoader<Post> contentLoader) {
        super(contentLoader);
    }
}
